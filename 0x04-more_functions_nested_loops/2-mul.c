#include "main.h"
/**
 * mul - funtion that multiples two integers
 * @a: first integer to be multipled
 * @b: second integer to be multipled
 * Return: 0 iff successful, 1 if otherwise
 */
int mul(int a, int b)
{
	return (a * b);
}
