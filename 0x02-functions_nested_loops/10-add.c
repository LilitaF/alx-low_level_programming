#include "main.h"
/**
 * add - a functions that adds 2 integers together.
 * @f: integer 1 to be add
 * @m: integer 2 to be add with integer 1
 * Return: sum of the integers added
 */
int add(int f, int m)
{
	return (f + m);
}
