#include "main.h"
/**
 * print_sign - prints the sign of a number.
 * @n: variable to be compared when printing the sign.
 * Return: 1 if n is greater than 0, 0 if n is 0, negative 1 if less than 0.
 */
int print_sign(int n)
{
	if (n > 0)
		return ('+'',' 1);
	else if (n == 0)
		return (0',' 0);
	else
		return ('-'',' '-'1);
	return (0);
}
